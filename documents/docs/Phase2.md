Hiện tại tôi đang làm một project liên quan đến việc xác định Root Cause của Issue, để xác định được Root Cause tôi cần các thông tin liên quan đến Issue và source code của Project. Thông tin liên quan đến Issue tôi có thể lấy tự động được, phần lấy source code của project tôi chỉ có danh sách các API của MCP. Công ty tôi có một Agent AI có hỗ trợ MCP giúp tôi tự động lấy ra được các đoạn mã có liên quan đến lỗi trong source code từ các đoạn log lỗi, nhưng mà để tương tác 1 cách tự động với Agent AI thì hiện tại chưa có cách nào cả, chỉ có thể tương tác thông qua extension trên VSCode. Hiện tại tôi có 1 LLM và API để gọi đến LLM đó và việc tôi cần làm hiện tại là sử dụng API đến LLM này để giải quyết bài toán tìm root cause cho đến khi AI Agent của chúng tôi hỗ trợ tương tác 1 cách tự động thông qua CLI. Hãy define cho tôi những phần tôi có thể làm với API của LLM và những phần cần AI Agent một cách rõ ràng.chan